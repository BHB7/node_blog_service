<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>您的验证码来啦~</title>
  <style>
    :root {
      --main-color: #ff7ab5;
      --text-color: #333;
      --bg-color: #fefefe;
      --gray: #888;
    }
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', 'PingFang SC', sans-serif;
      background: #f5f5f5;
      color: var(--text-color);
    }
    .container {
      max-width: 480px;
      margin: 40px auto;
      background: var(--bg-color);
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
      padding: 24px;
    }
    .header {
      text-align: center;
      font-size: 22px;
      font-weight: bold;
      color: var(--main-color);
      margin-bottom: 10px;
    }
    .content {
      font-size: 16px;
      line-height: 1.8;
      margin-bottom: 20px;
    }
    .code-box {
      font-size: 28px;
      font-weight: bold;
      color: var(--main-color);
      background: #fff3f9;
      padding: 12px 16px;
      border-radius: 8px;
      text-align: center;
      letter-spacing: 2px;
      margin-bottom: 20px;
      user-select: all;
    }
    .button {
      display: block;
      width: 100%;
      background: var(--main-color);
      color: white;
      text-align: center;
      padding: 12px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: background 0.3s;
    }
    .button:hover {
      background: #ff579e;
    }
    .footer {
      text-align: center;
      font-size: 13px;
      color: var(--gray);
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">Hi~ ❤</div>
    <div class="content">
      您的验证码来啦，请在 <strong>5 分钟内</strong>使用完成验证哦~
    </div>
    <div class="code-box" id="code"><%= code %></div>
    <a href="#" class="button" onclick="copyCode(event)">点击复制验证码 ✨</a>
    <div class="footer">
      如果这不是您本人的操作，请放心忽略这封邮件~<br/>
      感谢使用我们的服务，祝你生活愉快🌸
    </div>
  </div>

  <script>
    function copyCode(e) {
      e.preventDefault();
      const codeText = document.getElementById('code').innerText.trim();
      navigator.clipboard.writeText(codeText).then(() => {
        alert('验证码已悄悄复制好啦~ 快去粘贴使用吧！✨');
      });
    }
  </script>
</body>
</html>
